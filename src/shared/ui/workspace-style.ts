import type {CSSProperties} from 'react';

export const GRIDDED_WORKSPACE_STYLE: CSSProperties = {
  backgroundColor: '#f1f5f9',
  backgroundImage:
    'linear-gradient(to right, rgba(255, 255, 255, 0.95) 1px, transparent 1px), linear-gradient(to bottom, rgba(255, 255, 255, 0.95) 1px, transparent 1px)',
  backgroundSize: '40px 40px',
};
