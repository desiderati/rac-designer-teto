/**
 * {{pascalCase name}} — Smoke Test
 *
 * Quick sanity check for the core logic of {{name}}.
 * Co-located with the source file for easy discovery.
 */

import { describe, it, expect } from 'vitest';

describe('{{pascalCase name}} (smoke)', () => {
  it('should exist and be importable', () => {
    // TODO: Replace with a meaningful smoke test
    expect(true).toBe(true);
  });
});
