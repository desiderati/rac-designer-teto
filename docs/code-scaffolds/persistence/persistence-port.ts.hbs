/**
* {{pascalCase model}} Persistence Port
*
* Contrato síncrono de persistência alinhado ao padrão atual do repositório.
* O domínio define o que precisa; a infraestrutura fornece a implementação.
*/

import type { {{pascalCase model}}State } from './{{kebabCase model}}.aggregate';

export interface {{pascalCase model}}PersistencePort {
load(): {{pascalCase model}}State | null;
save(state: {{pascalCase model}}State | null): void;
}
