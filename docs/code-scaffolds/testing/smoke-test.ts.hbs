/**
 * {{pascalCase name}} — Smoke Test
 *
 * Sanity check mínimo para a lógica principal de {{name}}.
 * Co-localizado com o arquivo fonte para facilitar descoberta e manutenção.
 */

import {describe, expect, it} from 'vitest';

describe('{{pascalCase name}} (smoke)', () => {
  it('mantém o comportamento base esperado', () => {
    // TODO: substituir por uma asserção comportamental mínima e real
    expect(true).toBe(true);
  });
});
