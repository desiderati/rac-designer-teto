

# Limpar modal "Posicionar Vista" (SideSelector)

Aplicar as mesmas mudancas feitas no HouseTypeSelector ao modal de posicionamento em `src/components/rac-editor/SideSelector.tsx`:

1. Remover o `DialogDescription` e `SheetDescription` (textos "Clique/Toque no lado da casa...")
2. Adicionar `hideCloseButton` ao `DialogContent`
3. Centralizar o titulo no Dialog (desktop)
4. Limpar imports nao utilizados (`DialogDescription`, `SheetDescription`)

### Arquivo editado
- `src/components/rac-editor/SideSelector.tsx`

