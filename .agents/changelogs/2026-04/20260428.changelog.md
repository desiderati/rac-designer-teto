# Changelog - 2026-04-28

---

## [21:31] Iniciar refatoração arquitetural do editor RAC

### Branch do projeto

- `codex/autonomous-loop/editor-architecture`

### Tipo

- arquitetura
- refatoração
- documentação
- teste

### Origem da demanda

- solicitação do usuário para executar loops autônomos com League of Agents e `autonomous-loop`

### Escopo afetado

- `docs/architecture-decisions/`
- `docs/superpowers/plans/`
- `src/components/rac-editor/contracts/`
- `src/components/rac-editor/store/`
- `src/components/rac-editor/canvas/ports/`
- `src/components/rac-editor/lib/canvas/piloti-selection.ts`
- `OBSIDIAN.md`
- `docs/README.md`

### Contexto

A refatoração arquitetural busca reduzir vazamento de Fabric.js em UI, hooks e coordenação de estado, preparar
commands/store e ports/adapters locais do editor, e manter o domínio independente de runtime gráfico.

### Decisão tomada

Adotar migração incremental feature-local, com ADR `proposed`, contracts serializáveis, store inicial de seleção,
ports mínimos de canvas e primeira fatia de piloti compatível com o contrato legado.

### Alterações realizadas

- Criado `ADR-001-fronteira-editor-runtime-fabric.md` como decisão proposta.
- Criado plano operacional em `docs/superpowers/plans/2026-04-28-editor-architecture-refactor.md`.
- Adicionados contratos serializáveis para ids, seleção, vista e draft de contraventamento.
- Adicionados commands e `EditorStore` inicial para seleção serializável.
- Adicionados ports de evento, render e documento de canvas.
- `PilotiCanvasSelection` passou a carregar `editorSelection` serializável como campo transitório.
- `docs/README.md` e `OBSIDIAN.md` passaram a indexar ADRs.

### Validação

- `rtk npm run test`: passou no baseline inicial, 69 arquivos e 181 testes.
- `rtk npm exec -- vitest run src/components/rac-editor/contracts src/components/rac-editor/store src/components/rac-editor/canvas/ports src/components/rac-editor/lib/canvas/piloti-selection.smoke.test.ts`: passou, 6 arquivos e 13 testes.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou com aviso pré-existente em `House3DScene.tsx`.
- `rtk git diff --check`: sem problemas.
- `rg` não encontrou `fabric`, `CanvasGroup`, `CanvasObject`, `FabricCanvas` ou `FabricObject` nos novos contracts/store/ports.

### Regressão?

- não observada nesta etapa

### Necessita documentação derivada?

- sim, ADR proposto e plano operacional foram criados

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- Consumers reais ainda usam contratos legados com `CanvasGroup` e `CanvasHandle.canvas`.
- O próximo loop deve migrar consumidores, não apenas criar contratos.
- `houseManager` ainda concentra estado, runtime canvas e persistência transitória.

### Tags

#rac-editor #architecture #fabric #ports-adapters #teste #documentacao

---

## [22:06] Expandir loops de migração real dos consumidores do editor

### Branch do projeto

- `codex/autonomous-loop/editor-architecture`

### Tipo

- arquitetura
- refatoração
- teste

### Origem da demanda

- continuidade da execução autônoma solicitada pelo usuário para ir além da fatia inicial de piloti

### Escopo afetado

- `src/components/rac-editor/application/`
- `src/components/rac-editor/hooks/usePilotiEditor.ts`
- `src/components/rac-editor/ui/modals/editors/piloti/PilotiEditor.tsx`
- `src/components/rac-editor/hooks/canvas/useCanvasEditorEvents.ts`
- `src/components/rac-editor/hooks/modals/useGenericObjectEditor.ts`
- `src/components/rac-editor/hooks/toolbar/useToolbarHouseViewCounts.ts`
- `src/components/rac-editor/lib/canvas/canvas.ts`
- `src/components/rac-editor/ui/RacEditor.tsx`
- `src/components/rac-editor/ui/canvas/Canvas.tsx`

### Contexto

A primeira rodada criou contratos e ports. Esta etapa migrou consumidores reais para começar a reduzir dependências
diretas de `CanvasGroup`, `houseManager` e referências vivas de objetos de canvas em fluxos de UI.

### Decisão tomada

Manter os contratos legados por compatibilidade, mas anexar DTOs serializáveis (`editorSelection`) e introduzir
ports de aplicação/read model com adapters legados. A fronteira nova passa a ser consumida por piloti, editores
genéricos e toolbar sem exigir reescrita completa do runtime Fabric.

### Alterações realizadas

- `RacEditor` passou a compor `EditorStoreProvider` internamente.
- `PilotiEditor` e `usePilotiEditor` deixaram de receber `CanvasGroup`.
- Criados `PilotiEditorPort`, `legacyPilotiEditorPort`, `HouseReadPort` e `legacyHouseReadPort`.
- Seleções de parede, linha/seta/distância e terreno passaram a carregar `editorSelection` serializável.
- Objetos Fabric receberam `editorObjectId` persistível para identidade pública estável.
- `useGenericObjectEditor` e seleção de terreno passaram a despachar commands para `EditorStore`.
- Toolbar passou a ler contagens de vistas por `HouseReadPort`.

### Validação

- `rtk npm run test`: passou, 75 arquivos e 197 testes.
- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou com o warning pré-existente em `House3DScene.tsx`.
- `rtk git diff --check`: sem problemas.
- `rg` não encontrou `fabric`, `CanvasGroup`, `FabricCanvas` ou `FabricObject` em contracts, store,
  canvas ports, application ports, `usePilotiEditor` ou modal de piloti.

### Regressão?

- não observada nos testes automatizados executados

### Necessita documentação derivada?

- sim, work-item atualizado com checkpoints e snapshot pós-rodada

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- `CanvasHandle.canvas` ainda é escape hatch em vários hooks.
- `houseManager` ainda concentra escrita, persistência e rebuild.
- Import/export e histórico continuam baseados em JSON Fabric.
- Contraventamento possui contratos, mas comandos visuais ainda dependem de `CanvasGroup`.

### Tags

#rac-editor #architecture #fabric #ports-adapters #refatoracao #teste

---

## [23:08] Convergir estrutura do editor e reduzir primeira fatia de `CanvasHandle.canvas`

### Branch do projeto

- `codex/autonomous-loop/editor-architecture`

### Tipo

- arquitetura
- refatoração
- teste
- documentação operacional

### Origem da demanda

- continuidade aprovada pelo usuário após validação do desenho de diretórios e dos próximos ciclos

### Escopo afetado

- `src/bootstrap/`
- `src/components/rac-editor/canvas/`
- `src/components/rac-editor/store/`
- `src/components/rac-editor/hooks/usePilotiActions.ts`
- `src/components/rac-editor/ui/RacEditorCanvas.tsx`
- `src/components/rac-editor/ui/canvas/Canvas.tsx`
- `src/infra/house/`
- `.agents/refactorings/`
- `.agents/work-items/20260428-autonomous-loop-editor-architecture.work-item.assets/loop-state.md`

### Contexto

A rodada retomou a refatoração incremental para alinhar a estrutura de diretórios ao desenho validado na conversa e
começar a reduzir o escape hatch `CanvasHandle.canvas` por capacidades explícitas do editor.

### Decisão tomada

Adotar a estrutura `bootstrap`, `components/rac-editor/canvas`, `components/rac-editor/store` e `infra/house`.
`src/infra/canvas/FabricCanvasAdapter.ts` continua deliberadamente pendente até existir um port concreto a implementar.

### Alterações realizadas

- Contratos de editor foram consolidados em `src/components/rac-editor/canvas/types`.
- Ports de canvas passaram para `src/components/rac-editor/canvas`.
- Store, commands e application ports locais passaram para `src/components/rac-editor/store`.
- Bootstrap/contexto do editor passaram para `src/bootstrap`.
- Adapters legados de `houseManager` passaram para `src/infra/house`.
- `CanvasHandle` passou a expor capacidades pequenas de render, contagem de objetos ativos e feedback visual de piloti.
- `usePilotiActions.ts` e `RacEditorCanvas.tsx` deixaram de acessar `canvasRef.current.canvas` diretamente.
- Criados prompt, checklist, regression run e registro durável da frente de refatoração local.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- Testes focados de bootstrap/store/canvas/piloti: passaram, 9 arquivos e 20 testes.
- `rtk npm run test`: passou, 75 arquivos e 197 testes.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou com warning pré-existente em `House3DScene.tsx`.
- `rtk git diff --check`: sem problemas.
- `rg` não encontrou imports antigos de `application`, `contracts`, `canvas/ports` ou stores legados em `src`/`docs`.
- `rg` não encontrou `canvasRef.current.canvas` nos arquivos-alvo desta fatia.

### Regressão?

- não observada nos testes automatizados executados

### Necessita documentação derivada?

- sim, work-item e registro local de refatoração foram atualizados

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- Restam 13 acessos diretos a `CanvasHandle.canvas` em hooks/UI.
- `houseManager` e `RacEditor.tsx` ainda são pontos de concentração relevantes.
- Import/export, histórico e debug bridge continuam dependendo de Fabric/runtime visual.
- Validação visual manual do editor não foi executada nesta rodada.

### Tags

#rac-editor #architecture #fabric #ports-adapters #refatoracao #teste #autonomous-loop

---

## [23:25] Encapsular projeção e reset de canvas no `CanvasHandle`

### Branch do projeto

- `codex/autonomous-loop/editor-architecture`

### Tipo

- refatoração
- teste

### Origem da demanda

- continuidade do ciclo de redução de `CanvasHandle.canvas`

### Escopo afetado

- `src/components/rac-editor/ui/canvas/Canvas.tsx`
- `src/components/rac-editor/hooks/canvas/useCanvasTools.ts`
- `src/components/rac-editor/hooks/tutorial/useTutorialUiActions.ts`
- `.agents/refactorings/2026-04/20260428-2308-editor-runtime-boundary.*`
- `.agents/work-items/20260428-autonomous-loop-editor-architecture.work-item.assets/loop-state.md`

### Contexto

Após a convergência estrutural, ainda havia acessos diretos a `canvasRef.current.canvas` em hooks de tutorial,
ferramentas e debug. Esta rodada removeu a parte segura ligada a projeção de coordenadas e reset da superfície.

### Decisão tomada

Projeção de ponto de canvas, projeção de ponto local de grupo e reset da superfície passam a ser capacidades do
`CanvasHandle`. Os hooks de tutorial e ferramentas deixam de conhecer o objeto Fabric para essas operações.

### Alterações realizadas

- Adicionado `resetSurface` ao `CanvasHandle`.
- Adicionados `getCanvasPointScreenPosition` e `getGroupLocalPointScreenPosition` ao `CanvasHandle`.
- `useCanvasTools.ts` passou a posicionar balões por método do handle.
- `useTutorialUiActions.ts` passou a reiniciar a superfície e posicionar tutorial de piloti por método do handle.
- Inventário de acessos diretos a `CanvasHandle.canvas` caiu de 13 para 10 ocorrências.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- Testes focados de tutorial/projeção/bootstrap/canvas/store: passaram, 11 arquivos e 24 testes.
- `rtk npm run test`: passou, 75 arquivos e 197 testes.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou com warning pré-existente em `House3DScene.tsx`.
- `rtk git diff --check`: sem problemas.

### Regressão?

- não observada nos testes automatizados executados

### Necessita documentação derivada?

- sim, work-item e registro local de refatoração foram atualizados

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- Restam 10 acessos diretos a `CanvasHandle.canvas`.
- Os próximos acessos já envolvem escrita de objetos, inicialização do `houseManager` e debug bridge; exigem fronts mais cuidadosos.

### Tags

#rac-editor #fabric #ports-adapters #refatoracao #teste

---

## [23:58] Iniciar segunda etapa autônoma da fronteira do editor

### Branch do projeto

- `codex/autonomous-loop/editor-architecture-stage-2`

### Tipo

- refatoração
- teste
- documentação operacional

### Origem da demanda

- solicitação do usuário para disparar nova etapa de `autonomous-loop` após promover a worktree anterior para `main`

### Escopo afetado

- `src/components/rac-editor/hooks/canvas/useCanvasActions.ts`
- `src/components/rac-editor/hooks/canvas/useCanvasTools.ts`
- `src/components/rac-editor/lib/canvas/factory/elements/index.ts`
- `src/components/rac-editor/ui/RacEditor.tsx`
- `src/components/rac-editor/ui/canvas/Canvas.tsx`
- `.agents/work-items/20260428-autonomous-loop-editor-architecture-stage-2.work-item.assets/loop-state.md`

### Contexto

A primeira rodada foi promovida localmente para `main`. A segunda etapa iniciou pelo ponto de menor risco entre os
remanescentes: remover de `useCanvasTools` a dependência indireta de `getCanvas` e da criação via `FabricCanvas`.

### Decisão tomada

Criar capacidades explícitas no `CanvasHandle` para criação de elementos, adição centralizada no viewport visível e
alternância de modo desenho. O hook de ferramentas passa a orquestrar intenções de UI, não o runtime Fabric.

### Alterações realizadas

- `CanvasHandle` ganhou `createElementObject`, `addObjectAtVisibleCenter` e `setDrawingModeEnabled`.
- `useCanvasTools.ts` deixou de receber `getCanvas` e de importar `FabricCanvas`.
- `useCanvasActions.ts` passou a delegar adição de objeto e desativação do desenho ao handle.
- `RacEditor.tsx` deixou de passar `getCanvas`/`getVisibleCenter` para `useCanvasTools`.
- `ElementStrategyKey` passou a ser reexportado pelo barrel de factory de elementos.

### Validação

- `rtk npm install`: instalou dependências locais da nova worktree; reportou vulnerabilidades já presentes no grafo npm.
- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou após reexport de `ElementStrategyKey`.
- Testes focados: passaram, 4 arquivos e 8 testes.
- `rtk npm run lint`: passou com warning pré-existente em `House3DScene.tsx`.
- `rtk npm run build`: passou.
- `rtk git diff --check`: sem problemas.

### Regressão?

- não observada nos testes automatizados executados

### Necessita documentação derivada?

- sim, novo loop-state da stage 2 foi criado

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- `getCanvas` ainda é fornecido por `useCanvasActions` para import/export, PDF, house views e contraventamento.
- Restam 8 acessos diretos a `canvasRef.current.canvas`, concentrados em `useCanvasActions`, inicialização do `houseManager` e debug bridge.

### Tags

#rac-editor #fabric #ports-adapters #refatoracao #teste #autonomous-loop

---

## [23:34] Encapsular edição genérica de objetos no canvas

### Branch do projeto

- `codex/autonomous-loop/editor-architecture`

### Tipo

- refatoração
- teste

### Origem da demanda

- continuidade do ciclo de redução de `CanvasHandle.canvas`

### Escopo afetado

- `src/components/rac-editor/lib/canvas/generic-object-editor-strategy.ts`
- `src/components/rac-editor/lib/canvas/generic-object-editor-strategy.smoke.test.ts`
- `src/components/rac-editor/hooks/modals/useWallEditorActions.ts`
- `src/components/rac-editor/hooks/modals/useLinearEditorActions.ts`
- `src/components/rac-editor/ui/canvas/Canvas.tsx`
- `src/components/rac-editor/ui/modals/editors/generic/`

### Contexto

Os hooks dos editores genéricos ainda buscavam o objeto Fabric para aplicar alterações em muros, linhas, setas e
distâncias. A aplicação da edição é comportamento visual do canvas, não responsabilidade do modal.

### Decisão tomada

Mover a estratégia de edição genérica para `lib/canvas` e expor a aplicação pelo `CanvasHandle`, preservando os modais
como UI e deixando o handle cuidar de renderização e histórico.

### Alterações realizadas

- Criado `generic-object-editor-strategy.ts` em `lib/canvas`.
- Adicionado smoke test para edição de muro e distância.
- `CanvasHandle` ganhou `applyGenericObjectEdit`.
- `useWallEditorActions.ts` e `useLinearEditorActions.ts` deixaram de acessar `canvasRef.current.canvas`.
- Imports dos componentes de modal foram atualizados para o novo módulo.
- Inventário de acessos diretos a `CanvasHandle.canvas` caiu de 10 para 8 ocorrências.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- Testes focados: passaram, 10 arquivos e 22 testes.
- `rtk npm run test`: passou, 76 arquivos e 199 testes.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou com warning pré-existente em `House3DScene.tsx`.
- `rtk git diff --check`: sem problemas.

### Regressão?

- não observada nos testes automatizados executados

### Necessita documentação derivada?

- sim, work-item e registro local de refatoração foram atualizados

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- Restam 8 acessos diretos a `CanvasHandle.canvas`.
- As próximas frentes tocam inicialização do `houseManager`, debug bridge e comandos de canvas.

### Tags

#rac-editor #fabric #ports-adapters #refatoracao #teste
