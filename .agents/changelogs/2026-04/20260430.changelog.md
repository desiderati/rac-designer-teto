# Changelog - 2026-04-30

---

## [09:34] Consolidar identidade lógica das vistas da casa

### Branch do projeto

- `main`

### Tipo

- refatoração
- arquitetura
- teste
- documentação operacional

### Origem da demanda

- continuidade aprovada pelo usuário para aplicar as correções semânticas pendentes e finalizar com commit

### Escopo afetado

- `src/shared/types/house.ts`
- `src/shared/types/house-rebuild.ts`
- `src/domain/house/house.aggregate.ts`
- `src/domain/house/use-cases/house-views.use-case.ts`
- `src/domain/house/use-cases/house-views-rebuild.use-case.ts`
- `src/domain/house/house-persistence.port.ts`
- `src/infra/persistence/in-memory-house-persistence.adapter.ts`
- `src/components/rac-editor/lib/house-manager-*.ts`
- `src/components/rac-editor/lib/house-runtime-snapshot.ts`
- `src/components/rac-editor/ports/HouseStatePort.ts`
- testes de domínio, persistence, runtime e canvas

### Decisão tomada

- `HouseId` identifica a casa.
- `HouseViewInstanceId` identifica uma instância visual da casa.
- `HouseState` e `HouseAggregate` deixaram de ser genéricos.
- `HouseViewInstance` passou a representar apenas identidade lógica e lado.
- `HouseRuntimeViewInstance<TGroup>` e `HouseRuntimeViews<TGroup>` ficaram responsáveis pela projeção com grupo visual.
- O registry de runtime do canvas agora indexa grupos por `HouseViewInstanceId`, não por um `viewRefId` transitório.

### Alterações realizadas

- Removidos `HouseState<T>`, `HouseAggregate<T>`, `HouseViews<T>` e `HouseViewInstance<T>` do domínio canônico.
- Separados tipos lógicos (`HouseViews`) de tipos de runtime (`HouseRuntimeViews`).
- Atualizados rebuild, remoção, limpeza de vistas obsoletas e snapshot runtime para operar por `instanceId`.
- Ajustados adapters de persistência para persistirem apenas `HouseState`.
- Atualizados serviços do `house-manager` e helpers de canvas para consumir `HouseRuntimeSnapshot` onde há grupo concreto.
- Atualizados testes de caracterização para refletir a nova separação lógica/runtime.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- Testes focados de domínio/manager/infra: passaram, 12 arquivos e 53 testes.
- `rtk npm run test`: passou, 85 arquivos e 220 testes.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou.
- `rtk git diff --check`: passou.
- `rtk rg` não encontrou `HouseState<`, `HouseAggregate<`, `HouseViews<`, `HouseViewInstance<`, `viewRefId` nem `rebuildViewsFromMappedSources` em `src`.

### Regressão?

- não observada nos testes automatizados executados

### Riscos ou pendências

- `HouseWritePort` ainda aceita `CanvasGroup` nas operações de registrar/remover vistas porque essa fronteira continua servindo o canvas atual.
- `HouseStatePort` expõe `HouseRuntimeSnapshot` para compatibilidade com a UI; o nome pode ser revisitado quando a leitura reativa for separada em lógica/runtime.

### Tags

#rac-editor #house-manager #house-state #ports-adapters #refatoracao

---

## [10:12] Prefixar slices principais do RAC editor com `@`

### Branch do projeto

- `main`

### Tipo

- refatoração
- organização de diretórios
- teste
- documentação operacional

### Origem da demanda

- experimento aprovado pelo usuário para agrupar melhor no explorer os quatro slices principais dentro de `rac-editor`

### Escopo afetado

- `src/components/rac-editor/@canvas/**`
- `src/components/rac-editor/@menus/**`
- `src/components/rac-editor/@modals/**`
- `src/components/rac-editor/@viewer-3d/**`
- `src/components/rac-editor/hooks/**`
- `src/components/rac-editor/lib/**`
- `src/components/rac-editor/ui/**`
- `src/bootstrap/editor-bootstrap.ts`
- `src/infra/house/**`
- `README.md`
- `docs/architecture-decisions/ADR-001-fronteira-editor-runtime-fabric.md`
- `docs/engineering-playbook/PLAY-004-project-structure.md`
- `docs/engineering-playbook/PLAY-005-naming-conventions.md`
- `docs/engineering-playbook/PLAY-102-frontend-state-and-hooks.md`

### Decisão tomada

- Prefixar apenas os quatro slices principais de produto/feature: `@canvas`, `@menus`, `@modals` e `@viewer-3d`.
- Manter subdiretórios técnicos sem prefixo: `hooks`, `lib`, `ports`, `store` e `ui`.
- Preservar alterações pendentes existentes em `viewer3d`, incluindo a organização de parsers sob `lib/parsers`.

### Alterações realizadas

- Renomeados os diretórios físicos dos quatro slices.
- Atualizados imports absolutos para os novos paths com `@`.
- Atualizada documentação corrente para refletir a nova organização.
- Mantidas intactas as mudanças pendentes que já existiam no working tree antes deste ciclo.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- Testes focados dos quatro slices e bootstrap/manager: passaram, 59 arquivos e 146 testes.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou.
- `rtk git diff --check`: passou.
- `rtk rg` não encontrou referências antigas a `rac-editor/canvas`, `rac-editor/menus`, `rac-editor/modals` ou `rac-editor/viewer3d` em `src`, `docs` e `README.md`.

### Regressão?

- não observada nos testes automatizados executados

### Riscos ou pendências

- `@` em diretório físico funcionou com TypeScript, Vite, Vitest e ESLint; ainda vale observar ergonomia em auto-imports e navegação diária.
- O working tree já continha mudanças pendentes em `viewer3d` antes desta rodada; este ciclo preservou e deslocou essas mudanças para `@viewer-3d`.

### Tags

#rac-editor #organizacao #paths #refatoracao #explorer

---

## [11:13] Preservar checkpoint após troca acidental de branch

### Branch do projeto

- `main`

### Tipo

- checkpoint operacional
- refatoração de nomenclatura
- documentação operacional

### Origem da demanda

- o usuário informou que havia mudado de branch sem querer e retornou para a branch correta

### Escopo afetado

- `.agents/**`
- `.codex/**`
- `AGENTS.md`
- `CLAUDE.md`
- `CONTRIBUTING.md`
- `src/components/rac-editor/@canvas/ui/**`
- `src/components/rac-editor/hooks/**`

### Decisão tomada

- preservar as alterações locais já presentes antes dos próximos oito ciclos autônomos.
- tratar os renomes de hooks de contraventamento como checkpoint prévio, não como o primeiro ciclo arquitetural.

### Alterações realizadas

- mantidos renomes para nomes mais explícitos ligados a canvas/editor em hooks de contraventamento.
- preservadas atualizações operacionais de agentes e continuidade local.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- testes focados em hooks de canvas/editor: passaram.

### Regressão?

- não observada na validação executada.

### Riscos ou pendências

- este checkpoint não substitui os oito ciclos arquiteturais planejados; ele apenas limpa a base antes deles.

### Tags

#checkpoint #branch #rac-editor #agents #continuidade

---

## [11:17] Tornar snapshot de runtime da casa genérico

### Branch do projeto

- `main`

### Tipo

- refatoração
- arquitetura
- ports/adapters

### Origem da demanda

- ciclo 1 de 8 da decomposição contínua do `rac-editor`

### Escopo afetado

- `src/components/rac-editor/lib/house-runtime-snapshot.ts`
- `src/components/rac-editor/ports/HouseStatePort.ts`
- `src/components/rac-editor/lib/house-manager-*.ts`
- `src/components/rac-editor/@canvas/lib/house-3d-projection.ts`
- `src/infra/house/house-manager-state-adapter.ts`
- `.agents/refactorings/2026-04/20260430-1117-house-runtime-snapshot-generico.refactoring.md`

### Decisão tomada

- `HouseRuntimeSnapshot` e `HouseStatePort` agora são genéricos.
- consumidores que ainda precisam de grupos visuais declaram explicitamente `HouseRuntimeSnapshot<CanvasGroup>`.

### Alterações realizadas

- removido import direto de `CanvasGroup` do contrato base de snapshot.
- especializada a porta de estado em `HouseStatePort<CanvasGroup>` no adapter atual.
- atualizados pontos de runtime que ainda dependem do grupo visual concreto.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- testes focados de house-manager, terrain e runtime: passaram, 3 arquivos e 13 testes.
- `rtk git diff --check`: passou.

### Regressão?

- não observada na validação executada.

### Riscos ou pendências

- `CanvasGroup` ainda permanece em serviços do `house-manager`; o próximo ciclo deve reduzir essa dívida por generics/adapters.

### Tags

#rac-editor #house-manager #runtime #ports-adapters #refatoracao

---

## [11:22] Generificar runtime visual do house-manager

### Branch do projeto

- `main`

### Tipo

- refatoração
- arquitetura
- ports/adapters

### Origem da demanda

- ciclo 2 de 8 da decomposição contínua do `rac-editor`

### Escopo afetado

- `src/components/rac-editor/lib/house-manager-runtime-port.ts`
- `src/components/rac-editor/lib/house-manager-canvas-runtime.ts`
- `src/components/rac-editor/lib/house-manager-query-service.ts`
- `src/components/rac-editor/@canvas/ports/CanvasHouseRuntimePort.ts`
- `.agents/refactorings/2026-04/20260430-1122-house-manager-runtime-generico.refactoring.md`

### Decisão tomada

- formalizar uma porta visual genérica para o runtime da casa.
- manter `CanvasGroup` como especialização da borda de canvas, não como obrigação do runtime genérico.

### Alterações realizadas

- criado `HouseVisualRuntimePort<TGroup>`.
- criado `HouseRuntimeGroupRef`.
- parametrizados `HouseManagerCanvasRuntime` e `HouseManagerQueryService`.
- `CanvasHouseRuntimePort` passou a estender a porta visual genérica com `CanvasGroup`.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- testes focados de house-manager, terrain, runtime e ports de canvas: passaram, 4 arquivos e 15 testes.
- `rtk git diff --check`: passou.

### Regressão?

- não observada na validação executada.

### Riscos ou pendências

- comandos e efeitos do `house-manager` ainda chamam helpers concretos de canvas; os próximos ciclos devem separar leitura lógica, runtime visual e efeitos.

### Tags

#rac-editor #house-manager #runtime #ports-adapters #refatoracao
