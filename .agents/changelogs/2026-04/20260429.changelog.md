# Changelog - 2026-04-29

---

## [01:04] Remover `CanvasHandle.canvas` e criar porta de escrita da casa

### Branch do projeto

- `main`

### Tipo

- refatoração
- arquitetura
- teste
- documentação operacional

### Origem da demanda

- continuidade aprovada pelo usuário para executar os próximos passos no branch atual, sem novas worktrees

### Escopo afetado

- `src/components/rac-editor/ui/canvas/Canvas.tsx`
- `src/components/rac-editor/hooks/canvas/useCanvasActions.ts`
- `src/components/rac-editor/hooks/canvas/useCanvasHouseInitialization.ts`
- `src/components/rac-editor/hooks/canvas/useCanvasHouseViewActions.ts`
- `src/components/rac-editor/hooks/useRacEditorDebugBridge.ts`
- `src/components/rac-editor/hooks/useRacEditorPdfExportAction.ts`
- `src/components/rac-editor/ui/RacEditor.tsx`
- `src/components/rac-editor/store/HouseWritePort.ts`
- `src/infra/house/legacy-house-write-adapter.ts`
- `src/components/rac-editor/lib/house-manager.ts`
- `src/components/rac-editor/lib/house-manager-terrain.ts`
- `.agents/work-items/20260428-autonomous-loop-editor-architecture-stage-2.work-item.assets/loop-state.md`

### Contexto

A etapa continuou a redução do acoplamento entre UI/hooks e Fabric.js/`houseManager`, agora no branch `main` e sem
worktree. O objetivo imediato foi fechar o campo público `CanvasHandle.canvas` e criar uma primeira porta de escrita
para terreno, família, deleção e fluxo de vistas.

### Decisão tomada

Remover `CanvasHandle.canvas` como propriedade pública. Manter `getRuntimeCanvas()` como escape hatch deprecado e
explícito enquanto import/export, PDF, debug, contraventamento e inicialização do manager ainda dependem do runtime.
Escritas de casa passam por `HouseWritePort` e adapter legado, preservando comportamento sem acoplar UI ao singleton.

### Alterações realizadas

- `CanvasHandle` ganhou `getRuntimeCanvas()` deprecado e `deleteActiveObjects`.
- `useCanvasActions`, `useCanvasHouseInitialization`, debug bridge e exportação PDF migraram para a nova fronteira.
- Criado `HouseWritePort` e `legacyHouseWritePort`.
- `RacEditor`, `useCanvasActions` e `useCanvasHouseViewActions` deixaram de importar `houseManager` diretamente.
- Criados testes de contrato para o adapter de escrita.
- Extraídos helpers de terreno/elevations de `house-manager.ts` para `house-manager-terrain.ts`.
- Criados testes de caracterização para os helpers extraídos.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- Testes focados: passaram, 12 arquivos e 38 testes.
- `rtk npm run test`: passou, 78 arquivos e 205 testes.
- `rtk npm run lint`: passou com warning pré-existente em `House3DScene.tsx`.
- `rtk npm run build`: passou.
- `rtk git diff --check`: sem problemas.
- `rtk rg` não encontrou `CanvasHandle['canvas']` nem `canvasRef.current?.canvas` em `src/components/rac-editor`.

### Regressão?

- não observada nos testes automatizados executados

### Necessita documentação derivada?

- sim, loop-state da stage 2 foi atualizado

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- `getRuntimeCanvas()` ainda é usado como escape hatch transitório.
- `getCanvas` ainda alimenta import/export, PDF e contraventamento.
- `houseManager` ainda concentra registro/remoção de vistas, piloti, snapshot 3D e rebuild.

### Tags

#rac-editor #fabric #ports-adapters #refatoracao #teste #autonomous-loop

---

## 2026-04-29 12:03 - Ciclo autonomo de composicao de ports e decomposicao de editor

### Tipo

- refatoracao
- arquitetura
- teste
- documentacao operacional

### Origem da demanda

- continuidade solicitada pelo usuario para executar os proximos ciclos sem worktree

### Escopo afetado

- `src/bootstrap/editor-bootstrap.ts`
- `src/bootstrap/editor-context.tsx`
- `src/components/rac-editor/hooks/useRacEditorController.ts`
- `src/components/rac-editor/hooks/useRacEditorShellController.ts`
- `src/components/rac-editor/hooks/useRacEditorTutorialController.ts`
- `src/components/rac-editor/hooks/canvas/useCanvasHouseViewActions.ts`
- `src/components/rac-editor/lib/house-manager.facade.ts`
- `src/components/rac-editor/lib/house-manager-effects.ts`
- `src/domain/house/use-cases/house-views-layout.use-case.ts`
- `src/infra/house/house-manager-*-adapter.ts`

### Contexto

A etapa continuou a reducao dos pontos de acoplamento entre UI, `houseManager` e adapters. O objetivo foi concentrar
composicao no bootstrap, remover nomenclatura `legacy-*` da infraestrutura corrente e promover regras puras de vistas
para o dominio.

### Decisao tomada

- A UI passa a receber `HouseReadPort`, `HouseWritePort`, `HouseRuntimePort` e `HouseStatePort` pelo contexto de bootstrap.
- Os adapters sobre `houseManager` deixam de ter nome `legacy-*`; o legado real fica restrito ao singleton encapsulado.
- O controller principal do RAC editor segue como orquestrador, mas tutorial e shell ja estao em controllers proprios.
- Decisoes puras de insercao/empilhamento de vistas pertencem ao dominio, nao ao hook de canvas.

### Alteracoes realizadas

- Criado `EditorPortsContext` e injecao opcional de ports em `EditorStoreProvider`.
- Migrados hooks/UI de alto nivel para `useEditorPorts`.
- Renomeados adapters `legacy-house-*` para `house-manager-*`.
- Renomeado o port de piloti para `houseManagerPilotiEditorPort`.
- Extraidos `useRacEditorTutorialController` e `useRacEditorShellController`.
- Extraido `HouseManagerEffects` da fachada do manager.
- Promovidos `resolveHouseViewInsertion` e `calculateStackedViewPositions` para use case de dominio.
- Acrescentados testes de caracterizacao para as novas regras de vistas.

### Validacao

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- `rtk npm exec -- vitest run src/domain/house/use-cases/house-views-layout.use-case.smoke.test.ts src/components/rac-editor/lib/house-manager.smoke.test.ts`: passou, 2 arquivos e 21 testes.
- `rtk npm exec -- vitest run src/components/rac-editor/lib/rac-editor.smoke.test.ts src/components/rac-editor/lib/use-mobile.smoke.test.tsx src/components/rac-editor/lib/house-manager.smoke.test.ts src/infra/house/house-manager-state-runtime-adapter.smoke.test.ts src/infra/house/house-manager-write-adapter.smoke.test.ts`: passou, 5 arquivos e 19 testes.
- `rtk npm run test`: passou, 84 arquivos e 221 testes.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou com warning pre-existente em `House3DScene.tsx`.
- `rtk git diff --check`: sem erros de whitespace, mas o Git emitiu warnings locais de conversao LF -> CRLF.

### Regressao?

- nao observada nos testes automatizados executados

### Necessita documentacao derivada?

- sim, registro de refatoracao atualizado

### Necessita atualizacao de runbook?

- nao

### Riscos ou pendencias

- `useRacEditorController` ainda tem volume alto por montar o contrato completo de layout.
- `HouseManagerFacade` ainda e a fachada publica do estado da casa, embora continue perdendo responsabilidades internas.
- Alguns modulos de desenho em `lib/canvas` continuam Fabric-first por serem a infraestrutura visual atual.

### Tags

#rac-editor #ports-adapters #house-manager #refatoracao #autonomous-loop

---

## 2026-04-29 12:24 - Ciclo autonomo de contrato imperativo do canvas

### Tipo

- refatoracao
- arquitetura
- teste

### Origem da demanda

- continuidade dos ciclos autonomos no branch atual, sem worktree

### Escopo afetado

- `src/components/rac-editor/store/CanvasInteractionPort.ts`
- `src/components/rac-editor/store/CanvasSelectionPort.ts`
- `src/components/rac-editor/ui/canvas/Canvas.tsx`
- `src/components/rac-editor/hooks/*`
- `src/components/rac-editor/ui/RacEditorCanvas.tsx`
- `src/components/rac-editor/ui/RacEditorModalEditors.tsx`

### Contexto

Mesmo apos remover o acesso direto ao runtime Fabric, os hooks ainda importavam `CanvasHandle` e tipos de selecao a
partir do componente visual `Canvas.tsx`. Isso mantinha acoplamento desnecessario entre controladores de aplicacao e a
implementacao React do canvas.

### Decisao tomada

- O contrato imperativo do canvas passa a viver em uma porta propria: `CanvasInteractionPort`.
- Tipos de selecao emitidos pelo canvas passam a viver em `CanvasSelectionPort`.
- `Canvas.tsx` implementa esses contratos, mas deixa de ser a fonte dos tipos consumidos por hooks.

### Alteracoes realizadas

- Criado `CanvasInteractionPort` com o contrato anteriormente exposto por `CanvasHandle`.
- Criado `CanvasSelectionPort` para selecoes de parede, linha, terreno e contraventamento.
- Atualizados hooks, modais e `RacEditorCanvas` para importar contratos de `store/*Port`.
- Removida a declaracao de `CanvasHandle` e dos tipos de selecao de `Canvas.tsx`.

### Validacao

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- `rtk npm exec -- vitest run src/bootstrap/editor-context.smoke.test.tsx src/components/rac-editor/ui/canvas/adapters/fabric-canvas-command-port.smoke.test.ts src/components/rac-editor/ui/canvas/adapters/hooks/useCanvasSelectionActions.smoke.test.ts src/components/rac-editor/lib/rac-editor.smoke.test.ts`: passou, 4 arquivos e 8 testes.

### Regressao?

- nao observada nos testes automatizados executados

### Necessita documentacao derivada?

- sim, registro de refatoracao atualizado

### Necessita atualizacao de runbook?

- nao

### Riscos ou pendencias

- `Canvas.tsx` ainda e grande e concentra a composicao de hooks Fabric; isso e esperado enquanto ele for o adapter visual principal.
- O proximo corte natural e extrair a montagem do contrato de layout do `useRacEditorController`.

### Tags

#rac-editor #canvas #ports-adapters #refatoracao

---

## 2026-04-29 12:30 - Ciclo autonomo de toolbar e sessao do manager

### Tipo

- refatoracao
- arquitetura
- teste

### Origem da demanda

- continuidade dos ciclos autonomos para reduzir `useRacEditorController` e a fachada do `houseManager`

### Escopo afetado

- `src/components/rac-editor/hooks/useRacEditorController.ts`
- `src/components/rac-editor/hooks/toolbar/useRacEditorToolbarController.ts`
- `src/components/rac-editor/lib/house-manager.facade.ts`
- `src/components/rac-editor/lib/house-manager-session-service.ts`

### Contexto

O controller principal ainda concentrava a composicao direta do modelo da toolbar e do read model da casa. A fachada do
manager ainda carregava diretamente a hidratacao e sincronizacao de sessao/familia/alturas.

### Decisao tomada

- A composicao de toolbar passa a ter controller proprio.
- A sessao do manager passa a ser coordenada por `HouseManagerSessionService`.
- A fachada continua publica, mas delega mais uma responsabilidade interna.

### Alteracoes realizadas

- Criado `useRacEditorToolbarController`.
- Criado `HouseManagerSessionService`.
- `HouseManagerFacade` delega reset, familia, alturas selecionadas e sync de projeto ao novo servico.

### Validacao

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- `rtk npm exec -- vitest run src/components/rac-editor/lib/house-manager.smoke.test.ts src/infra/house/house-manager-state-runtime-adapter.smoke.test.ts src/infra/house/house-manager-write-adapter.smoke.test.ts`: passou, 3 arquivos e 17 testes.

### Regressao?

- nao observada nos testes automatizados executados

### Necessita documentacao derivada?

- sim, registro de refatoracao atualizado

### Necessita atualizacao de runbook?

- nao

### Riscos ou pendencias

- `useRacEditorController` ainda monta o contrato completo do layout.
- A fachada do manager ainda contem delegacoes publicas amplas por compatibilidade com adapters.

### Tags

#rac-editor #house-manager #refatoracao #ports-adapters

---

## 2026-04-29 12:36 - Ciclo autonomo de builder do layout do editor

### Tipo

- refatoracao
- arquitetura
- teste

### Origem da demanda

- continuidade da decomposicao do `useRacEditorController`

### Escopo afetado

- `src/components/rac-editor/hooks/useRacEditorController.ts`
- `src/components/rac-editor/hooks/buildRacEditorLayoutProps.ts`

### Contexto

O controller principal ainda continha a traducao completa dos estados e handlers para `RacEditorLayoutProps`, misturando
orquestracao de fluxos com montagem de props visuais.

### Decisao tomada

- A montagem do contrato de layout passa para um builder tipado.
- O controller continua responsavel por compor fluxos e handlers, mas nao por estruturar a arvore de props do layout.

### Alteracoes realizadas

- Criado `buildRacEditorLayoutProps`.
- Substituido o bloco de retorno aninhado do controller por chamada ao builder.

### Validacao

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- `rtk npm exec -- vitest run src/components/rac-editor/lib/rac-editor.smoke.test.ts src/bootstrap/editor-context.smoke.test.tsx src/components/rac-editor/ui/canvas/adapters/hooks/useCanvasSelectionActions.smoke.test.ts`: passou, 3 arquivos e 4 testes.

### Regressao?

- nao observada nos testes automatizados executados

### Necessita documentacao derivada?

- sim, registro de refatoracao atualizado

### Necessita atualizacao de runbook?

- nao

### Riscos ou pendencias

- O controller ainda tem muitos fluxos por ser o orquestrador de tela; novas reducoes devem atacar grupos de fluxo, nao apenas mover linhas.

### Tags

#rac-editor #layout #refatoracao

---

## [07:37] Decompor `RacEditor.tsx` e `house-manager.ts` em raízes finas

### Branch do projeto

- `main`

### Tipo

- refatoração
- arquitetura
- teste
- documentação operacional

### Origem da demanda

- continuidade autônoma aprovada pelo usuário, sem criação de worktree, até decompor `RacEditor.tsx` e `house-manager.ts`

### Escopo afetado

- `src/components/rac-editor/ui/RacEditor.tsx`
- `src/components/rac-editor/ui/RacEditorContent.tsx`
- `src/components/rac-editor/ui/RacEditorLayout.tsx`
- `src/components/rac-editor/ui/RacEditor3DViewerOverlay.tsx`
- `src/components/rac-editor/lib/house-manager.ts`
- `src/components/rac-editor/lib/house-manager.facade.ts`
- `src/components/rac-editor/lib/house-manager-state.ts`
- `src/components/rac-editor/lib/house-manager-canvas-runtime.ts`
- `src/components/rac-editor/lib/house-manager-notifier.ts`
- `src/components/rac-editor/lib/house-manager-views.ts`
- `src/components/rac-editor/lib/house-manager-piloti.ts`
- `src/components/rac-editor/lib/house-manager-auto-effects.ts`
- `src/components/rac-editor/lib/house-manager-snapshot.ts`
- `src/components/rac-editor/lib/house-manager-session.ts`
- `src/components/rac-editor/store/HouseManagerCanvasPort.ts`
- `src/components/rac-editor/store/HouseWritePort.ts`
- `src/infra/house/legacy-house-write-adapter.ts`

### Contexto

A etapa atacou os dois arquivos reconhecidos como God Files. O objetivo não foi apenas reduzir linhas, mas explicitar
fronteiras: `RacEditor.tsx` como raiz de provider, `house-manager.ts` como export transitório do singleton, Fabric
isolado em adapter/runtime de canvas e regras de casa distribuídas em módulos coesos.

### Decisão tomada

- `RacEditor.tsx` ficou como casca de provider sobre `RacEditorContent`.
- A árvore visual foi movida para `RacEditorLayout` e o modal 3D lazy para `RacEditor3DViewerOverlay`.
- `house-manager.ts` passou a exportar somente a instância de `HouseManagerFacade`.
- A fachada passou a delegar estado/persistência, sessão, canvas runtime, notificação, views, piloti, snapshot e efeitos automáticos.
- Consumidores simples de UI migraram de `houseManager` direto para `HouseWritePort`, `HouseReadPort` ou `useHouseSnapshot`.

### Alterações realizadas

- Criados hooks de orquestração do editor para família, leitura da casa, settings, terreno, editores de objeto, documentos e toolbar.
- Criado `HouseManagerCanvasPort` e adapter Fabric específico para inicialização/rebuild/snapshot.
- Extraídos módulos de `house-manager`: state, session, canvas runtime, notifier, views, piloti, auto effects e snapshot.
- Migrados restart do tutorial, undo/rebuild, import JSON, terreno, snapshot 3D, alturas de piloti e contraventamento para portas/store.
- Atualizados testes do manager para inicializar via port de canvas.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- Testes focados: passaram, 13 arquivos e 54 testes.
- `rtk npm run test`: passou, 78 arquivos e 205 testes.
- `rtk npm run lint`: passou com warning pré-existente em `House3DScene.tsx`.
- `rtk npm run build`: passou.
- `rtk git diff --check`: sem problemas.

### Regressão?

- não observada nos testes automatizados executados

### Necessita documentação derivada?

- sim, loop-state e registro de refatoração foram atualizados

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- `getRuntimeCanvas()` ainda existe como escape hatch transitório para fluxos que ainda não ganharam portas específicas.
- `useRacEditorDebugBridge`, `house-store` e inicialização do manager ainda acessam o singleton por serem fronteiras legadas/debug.
- A fachada `HouseManagerFacade` ainda é grande, mas agora contém coordenação de fachada; a lógica pesada já está modularizada.

### Tags

#rac-editor #house-manager #ports-adapters #refatoracao #teste #autonomous-loop

---

## [10:36] Remover escape hatch do canvas e consolidar adapters/ports

### Branch do projeto

- `main`

### Tipo

- refatoração
- arquitetura
- teste
- documentação operacional

### Origem da demanda

- pedido explícito do usuário para continuar autonomamente os 8 pontos destacados, sem novas worktrees

### Escopo afetado

- `src/components/rac-editor/ui/canvas/Canvas.tsx`
- `src/components/rac-editor/ui/canvas/adapters/*`
- `src/components/rac-editor/hooks/useRacEditorController.ts`
- `src/components/rac-editor/hooks/useRacEditorCanvasController.ts`
- `src/components/rac-editor/hooks/useRacEditorContraventamentoController.ts`
- `src/components/rac-editor/lib/house-manager.facade.ts`
- `src/components/rac-editor/lib/house-manager-command-service.ts`
- `src/components/rac-editor/lib/house-manager-query-service.ts`
- `src/components/rac-editor/store/CanvasDocumentPort.ts`
- `src/components/rac-editor/store/CanvasDebugPort.ts`
- `src/components/rac-editor/store/HouseRuntimePort.ts`
- `src/components/rac-editor/store/HouseStatePort.ts`
- `src/domain/house/use-cases/house-piloti.use-case.ts`
- `src/infra/house/legacy-house-runtime-adapter.ts`
- `src/infra/house/legacy-house-state-adapter.ts`

### Contexto

A etapa removeu o escape hatch `getRuntimeCanvas()` e fechou os consumidores remanescentes de `getCanvas` por portas
explícitas. Também moveu hooks que manipulam Fabric para a área de adapters do canvas e continuou a decomposição da
fachada do `house-manager`.

### Decisão tomada

- Fabric fica visível apenas no runtime/adapters/factory de canvas, não em hooks de aplicação, store, domínio ou infra.
- `CanvasHandle` expõe capacidades específicas, não o runtime.
- `HouseManagerFacade` permanece como fronteira legada, mas delega leituras e comandos para serviços coesos.
- A regra de recomendação de piloti foi promovida para use case de domínio.

### Alterações realizadas

- Removido `getRuntimeCanvas()` do contrato público do canvas.
- Criados `CanvasDocumentPort`, `CanvasDebugPort`, `HouseRuntimePort` e `HouseStatePort`.
- Criados adapters Fabric para documento, debug, comandos e canvas port do house manager.
- Migrados JSON/PDF/debug/contraventamento/house-store/inicialização para ports e snapshots.
- Movidos hooks Fabric de `hooks/canvas` para `ui/canvas/adapters/hooks`.
- Extraídos `HouseManagerCommandService` e `HouseManagerQueryService`.
- Criados `useRacEditorController`, `useRacEditorCanvasController` e `useRacEditorContraventamentoController`.
- Atualizados testes de piloti para a tabela padrão atual de alturas `[1.0, 1.2, 1.5, 2.0, 2.5, 3.0]`.

### Validação

- `rtk npm exec -- tsc --noEmit --project tsconfig.app.json`: passou.
- `rtk npm exec -- vitest run src/components/rac-editor/lib src/components/rac-editor/store src/components/rac-editor/ui/canvas/adapters src/domain/house src/infra/house`: passou, 66 arquivos e 184 testes.
- `rtk npm run build`: passou.
- `rtk npm run lint`: passou com warning pré-existente em `House3DScene.tsx`.
- `rtk rg` não encontrou `getRuntimeCanvas` nem `getCanvas:` em `src`.
- `rtk rg` não encontrou imports de `fabric` em `src/components/rac-editor/hooks`, store, domínio, infra ou `Canvas.tsx`.

### Regressão?

- não observada nos testes automatizados executados

### Necessita documentação derivada?

- sim, loop-state e registro de refatoração foram atualizados

### Necessita atualização de runbook?

- não

### Riscos ou pendências

- ainda há Fabric nos módulos legados de desenho/factory em `lib/canvas`, o que é esperado enquanto esses módulos forem a infra visual.
- `houseManager` ainda existe como singleton legado, mas seus consumidores diretos ficaram restritos a adapters de infra e testes.
- `useRacEditorController` ainda é grande, embora já tenha sido separado em controllers menores.

### Tags

#rac-editor #fabric #ports-adapters #refatoracao #teste #autonomous-loop
